class Tree {
  constructor(root = null) {
    this.root = root;
    this.children = [];
  }

  addChild(value) {
    /* add node to the tree */
  }

  contains(target) {
    /* BF traversal */
  }
}

module.exports = { Tree };
